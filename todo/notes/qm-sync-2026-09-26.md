---
mode: subagent
---

<!-- SPDX-License-Identifier: MIT -->
<!-- SPDX-FileCopyrightText: 2025-2026 Marcus Quinn -->
# qm 上游同步记录

每次 `repos/qm`（即 `yc-software/qm`）拉取远端后追加一行，标 `recorded:` 时间戳。
下次检查：对比"上次 local HEAD"和当前 `origin/main`，若差异非空则重跑同步流程。

## Sync log

- **recorded: 2026-09-26T13:24+0800**（worktree `feature/qm-sync-2026-09-26`）
  - local baseline: `95b5a6a9941ce517f478147b1c6fa8365a16afa0`
  - local new HEAD: `8adee4b06a595b061977de094208e5d8625b50b2`
  - advance: 415 commits, 1791 files changed, +248,462 / -41,142 lines
  - new upstream tags: `desktop-v0.1.0`, `v0.1.10`, `v0.1.11`, `v0.1.12`
  - network: 直连 `github.com:443` 不通；走 `http://127.0.0.1:29290` 代理可达
  - proxy config: 已在 `repos/qm` 仓库内设 `http.proxy=http://127.0.0.1:29290`
    （仅该 repo；未污染全局 gitconfig）
  - 关键变更概要：见下表"qm 主题" 与同期 `qm-next-optimization-2026-09-26.md`

## 下次检查

```bash
# 工作目录：repos/qm（在 aa 仓内，按需配代理）
cd <aa-root>/repos/qm

# 上游新 SHA（github.com:443 通常走 proxy）
git fetch origin
git rev-parse origin/main
# 期望：返回 SHA == 本节最新 local HEAD（8adee4b0...），即无新提交；
# 否则对比上次记录 SHA 与当前 HEAD 之间的 commit 数重跑本流程。

# 或用 GitHub API（无需代理）：
curl -sS "https://api.github.com/repos/yc-software/qm/commits?per_page=1" \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d[0]['sha'])"
```

## qm 主题（415 commits 摘要）

按 commit 标题前缀统计（出现 ≥2 次）：

| 数量 | 前缀 |
|----:|------|
| 96 | `fix` |
| 31 | `web-ui` |
| 20 | `feat` |
| 12 | `test` |
| 6 | `docs` / `refactor` |
| 4 | `perf` / `build` |
| 3 | `sandbox` / `deploy` |
| 2 | `style` |

按子域归并（去重）：

| 子域 | 关键 commits |
|------|-------------|
| **桌面应用** | `Add QM desktop app with browser sign-in handoff (#1491)`、`Put the desktop download in Settings` |
| **Swarm 会话** | `Add swarm sessions`、`feat/swarm-coordination-followup`、`feat/split-subagents-sessions-tools` |
| **Inbox Loops** | `Add flagged Inbox Loops and durable event ingestion` |
| **多沙箱后端** | `Align the E2B/Modal/Sprites/Smolmachines backend with …`、`Porter sandbox backend`、`agent37 sandbox backend` |
| **Admin Lit 化** | `Migrate admin tabs to Lit and streamline Users and Slack settings`、`Redesign admin pages without comparison scaffolding` |
| **凭据 / 隔离** | `Native credential approvals`、`OAuth connection cards`、`Composio per-user`、`Add opt-in external Slack isolation and private continuation` |
| **可观测 / 性能** | `Add sampled Sentry performance tracing`、`Serve spend reports from saved daily totals`、`perf: reduce send latency during concurrent streams` |
| **Tape / 持久化** | `Preserve exact transcripts on the session tape`、`Tape parity`、`Recover failed compaction with bounded recent context` |
| **模型** | `GPT-6 Sol/Luna/Astra`、`claude-opus-5-5`、`Update bundled Codex 0.155.0`、`Hydrate OpenRouter on cold runtime` |
| **部署** | `Make AWS RDS instance class configurable`、`Consolidate web/admin and portal/auth into three application workloads`、`Support overlapping Sprites proxy hosts during migration` |

## 新增模块（顶级）

- `desktop/` — Electron 桌面 app（独立发布 `desktop-v0.1.0`）
- `superserve/templates/` — Superserve 沙箱模板
- `deploy/helm/` — Helm chart
- `deploy/connector-sdk/` — 连接器 SDK（独立 package）

## 新增核心子系统

- `src/swarms/` — Swarm 会话（多 agent 协同：`swarm-service`, `swarm-store`, `swarm-settings`, `swarm-fence`, `swarm-orchestrator`）
- `src/loops/` — Inbox Loops（持续后台 agent；`item-ledger`, `runner`, `governor`, `ship-gate`, `output-store`, `success-evaluation`, `sources/{adapter,gmail,slack}`）
- `src/model/` — 模型网关（`gateway-catalog`, `gateway-models`, `model-overlay`, `model-verification`, `model-lookup`）
- `src/harness/` — 通用化（`harness-shared`, `runtime-control`, `runtime-recovery`, `tape-projection`, `document-inputs`, `history-summary`, `runtime-types`；`pi-tools.ts` → `agent-tools.ts`）
- `src/sandbox/` — E2B / Modal / Superserve 后端 + `exec-sandbox-base` 抽象 + `sandbox-resources` + `connector-sdk` + `home-snapshot` + `layer-tool-install`

## 关键重命名 / 移动 / 删除

- `src/harness/pi-tools.ts` → `src/harness/agent-tools.ts`（去 Pi 命名）
- `src/cron/cron-fire-store.ts` → `src/cron/fire-store.ts`
- `porter/apps/*.yaml` → `porter/legacy/*.yaml`
- 删除：`src/skills/materialization-paths.ts`、`src/slack/core-bridge.ts`、`src/insights/reach-denied-notifier.ts`、`fly/fly.toml`、`scripts/predeploy-db-snapshot.sh`、`scripts/prod-deploy-guard.sh`、`deploy/sandbox/fly.toml`

## qm-next 对齐缺口（一句话版）

qm-next 不是 fork，而是 Cordis 重写 + 主动 parity 跟踪
（详见 `repos/qm-next/docs/parity-deviations.md` 1155 行 + `CHANGELOG.md`）。
qm-next 当前 HEAD = `acd05fd`（2026-09-22），正在做 qm-soul 层
（ADR-0018）+ Command Gate X3b 批 6 + ToolContext T1-T4 + Q3/Q4 段序补齐。

完整分析见同期 `qm-next-optimization-2026-09-26.md`。